[pytest]
testpaths = tests
# tests/ is on sys.path so `from conftest import ...` and the project modules
# both resolve without installing the package
pythonpath = . tests
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
